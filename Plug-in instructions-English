解析Json
代码只有一个JsonComponent

使用方法
新建蓝图后添加组件 Json 组件有4个函数

{
  "姓名": "张三",
  "年龄": 25,
  "城市": "北京",
  "在校生": false,
  "爱好": ["游泳", "阅读", "旅行"],
  "地址": {
    "省份": "北京市",
    "区域": "朝阳区",
    "街道": "建国路"
  }
}


GetJson: Takes a JSON object as input and returns an FJsonLib type.

GetJsonString: Takes an FJsonLib and a Key as input, retrieving the corresponding string value. For example, if the Key is "姓名" (Name), it retrieves "张三" (Zhang San).

GetJsonObj: Takes an FJsonLib and a Key as input, returning the corresponding string value. For instance, if the Key is "地址" (Address), it yields a new FJsonLib object representing the address.

GetJsonArray: Takes an FJsonLib and a Key as input, producing the corresponding string array. For instance, if the Key is "爱好" (Hobbies), it generates an array containing ["游泳" (Swimming), "阅读" (Reading), "旅行" (Traveling)].
